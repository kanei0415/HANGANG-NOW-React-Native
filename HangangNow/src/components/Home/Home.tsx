import colors from '@assets/colors';
import React from 'react';
import { ScrollView } from 'react-native';

const Home = () => {
  return (
    <ScrollView
      style={{ flex: 1, backgroundColor: colors.default.white }}></ScrollView>
  );
};

export default Home;
